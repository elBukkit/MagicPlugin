# Use this for an NPC Recall menu with no warmup or sp earn.
warpmenu:
  earns_sp: 0
  inherit: recall
  hidden: true
  actions:
      cast:
      - class: Recall
      alternate_down:
      alternate_up:
      alternate_sneak:
  parameters:
      cooldown: 5000
      allow_marker: false
      allow_death: true

