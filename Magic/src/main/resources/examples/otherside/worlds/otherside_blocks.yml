otherside_blocks:
  copy: world
  world: world_other
  autoload: true
  chunk_generate:
    replace:
      class: ReplacePopulator
      replace: otherside

otherside_blocks_backup:
  inherit: otherside_blocks
  world: world_netherside_backup
  autoload: false
