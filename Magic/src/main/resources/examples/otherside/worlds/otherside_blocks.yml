otherside_blocks:
  copy: world
  time_offset: 12000
  world: world_other
  autoload: true
  chunk_generate:
    replace:
      class: ReplacePopulator
      replace: otherside

otherside_blocks_backup:
  inherit: otherside_blocks
  world: world_other_backup
  autoload: false
