jumping:
  passive: true
  triggers: jump
  actions:
    alternate_sneak:
      - class: ChangeContext
        source_use_movement_direction: true
        actions:
        - class: Velocity
          additive: true
  parameters:
    target: self
    speed: jump_strength
