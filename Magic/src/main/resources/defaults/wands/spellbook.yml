spellbook:
    mode: skills
    icon: wood_hoe:31
    indestructible: true
    right_click: toggle
    undroppable: true
    keep: true
    bound: true
    class: mage
    # This parameter in skills mode determines if the skill items are quick cast
    # or if they must be held and right-clicked to cast.
    quick_cast: true